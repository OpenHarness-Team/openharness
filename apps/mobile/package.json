{
  "name": "@openharness/mobile",
  "version": "0.0.1",
  "description": "PARKED. Long-term mobile remote-control client that connects to a running Desktop/Host over HTTP/WebSocket. Re-evaluated after the desktop carrier architecture ships.",
  "private": true,
  "scripts": {
    "dev": "echo parked: see docs/refactor-plan.md Phase 5 && exit 0",
    "build": "echo parked: see docs/refactor-plan.md Phase 5 && exit 0",
    "typecheck": "echo parked && exit 0"
  }
}
