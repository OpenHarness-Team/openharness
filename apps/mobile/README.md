# @openharness/mobile (TODO scaffold)

Mobile client (React Native). Talks to the inference backend over a host
backend (HTTP/WebSocket). Not yet wired.

Android and iOS 鸿蒙 builds are supported. The app is scaffolded with Expo, but the native code is ejected to allow for custom native modules.


npx react-native@0.72.5 init mobile --version 0.72.5 --skip-install
